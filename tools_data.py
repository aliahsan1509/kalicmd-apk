# tools_data.py — the TOOLS database
# Paste the ENTIRE "TOOLS = [ ... ]" list (all ~85 entries) from my
# previous message here, exactly as written. That's the whole file.
TOOLS = [
    # <-- paste all tool dicts from the previous message's script here
]